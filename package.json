{
  "name": "voicefun",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check scripts/generate-story.mjs && node --check api/story.js"
  }
}
